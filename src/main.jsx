import React from "react";
import ReactDOM from "react-dom/client";
import TemperamentQuizApp from "./TemperamentQuizApp.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <TemperamentQuizApp />
  </React.StrictMode>
);
